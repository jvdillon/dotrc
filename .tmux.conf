# Use Ctrl+x as the prefix key to match .screenrc.
set -g prefix C-x
unbind C-b
bind C-x send-prefix

# Enable mouse support for scrolling, selecting, and resizing panes.
set -g mouse on

# Allow tmux to set the system clipboard via OSC 52.
set -g set-clipboard on

# Keep 50000 lines of scrollback history per pane.
set -g history-limit 50000

# Disable the visual and audible bell.
set -g visual-bell off
set -g bell-action none

# Let terminals report focus events to applications inside tmux.
set -g focus-events on

# Bind F-keys to common window operations without requiring the prefix.
bind -n F7 detach
bind -n F8 kill-window
bind -n F9 command-prompt "rename-window '%%'"
bind -n F10 new-window
bind -n F11 previous-window
bind -n F12 next-window

# Show a persistent status bar styled to match .screenrc's hardstatus.
set -g status-style bg=default,fg=green
set -g status-left-length 20
set -g status-left "#[bold,fg=brightgreen] Screen(s): "
set -g status-right "#[bold,fg=brightgreen,bg=black]%l:%M%p  %a, %b-%d-%Y "
set -g window-status-format "#[fg=blue]#I #W"
set -g window-status-current-format "#[fg=blue,bold]#I* #W"

# Add screen-style prefix bindings for kill, rename, clipboard, and copy mode.
bind k kill-window
bind A command-prompt "rename-window '%%'"
bind b run "tmux save-buffer - | xsel -i -b"
bind Escape copy-mode
